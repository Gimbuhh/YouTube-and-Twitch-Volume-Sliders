# Volume Sliders 2.4.6

- Fixes Twitch's on-video slider so a deliberate hover expands it even while Twitch keeps the native player controls hidden.
- Tracks overlay hover state explicitly, making interaction checks more reliable during player layout updates.
- Keeps the YouTube stationary-pointer guard intact while sharing the stronger hover-focus behavior.
